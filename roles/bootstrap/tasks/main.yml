---
- name: Create ansible user
  ansible.builtin.user:
    name: "{{ bootstrap_user.name }}"
    groups: sudo
    append: true
    shell: /bin/bash
    state: present

- name: Add ansible SSH key
  ansible.posix.authorized_key:
    user: "{{ bootstrap_user.name }}"
    key: "{{ bootstrap_user.ssh_public_key }}"
    state: present

- name: Allow ansible passwordless sudo
  ansible.builtin.copy:
    dest: "/etc/sudoers.d/{{ bootstrap_user.name }}"
    content: "{{ bootstrap_user.name }} ALL=(ALL) NOPASSWD: ALL\n"
    owner: root
    group: root
    mode: "0440"
    validate: "/usr/sbin/visudo -cf %s"
