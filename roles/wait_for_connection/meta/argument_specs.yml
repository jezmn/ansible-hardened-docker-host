---
argument_specs:
  main:
    short_description: Wait for the target to become reachable
    description:
      - Waits for the SSH connection to become usable (e.g. after a reboot)
        and re-gathers facts.
