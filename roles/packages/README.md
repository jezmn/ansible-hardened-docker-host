# packages

apt upgrade, base tools, UTC timezone, UDP tuning, fail2ban sshd jail, remove Snap.

Belongs to the `jezmn.ansible_hardened_docker_host` collection.
See the [root README](../../README.md) for the full role table and
[docs/configuration.md](../../docs/configuration.md) for its variables.
